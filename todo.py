#todo update the preprocess_text file to return the count of the vocabulary along with the features. stick with the keras sequence preprocessing for the time being but return the count
#todo make two different feature vectors for the audio involving both low level(rms,mean etc) and high level features(mfcc) and probably use two different transformer if necessary
#todo should i concat the audio transformer as well using cnn?
#todo get the facial keypoint dataset ready
#todo make the transformer ready for all by the next week and start training
#todo make the final dataset using os and pkl